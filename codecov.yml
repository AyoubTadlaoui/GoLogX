# Coverage is measured over the library (logx, audit) and the CLI (cmd/logx).
# The examples/ directory holds runnable demo programs with no test surface of
# their own, so it is excluded from the coverage total.
ignore:
  - "examples"

coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true
